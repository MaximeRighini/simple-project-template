"""
Instead of one monolithic script, code is grouped by business domain
(e.g., extraction, transformation, scoring).
This keeps each module self-contained, easy to navigate, and avoids merge conflicts
when multiple developers work in parallel.
"""
